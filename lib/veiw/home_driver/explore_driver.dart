import 'package:flutter/material.dart';

class ExploreDriverVeiw extends StatelessWidget {
  const ExploreDriverVeiw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class SignUpDriverVeiw extends StatelessWidget {
  const SignUpDriverVeiw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

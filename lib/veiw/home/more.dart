import 'package:flutter/material.dart';

class MoreInfoVeiw extends StatelessWidget {
  const MoreInfoVeiw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class ExploreVeiw extends StatelessWidget {
  const ExploreVeiw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
